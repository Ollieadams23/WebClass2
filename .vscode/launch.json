{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "debug node app",
            "program": "${workspaceFolder}/app.js"
        }
    ]
}
